apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  selector:
    project: roboshop
    component: frontend
    environment: dev
  ports:
    - protocol: TCP
      port: 80  #service Port
      targetPort: 80 #container Port

# three type of services are there ..
# cluster IP
# node port 
# load balancer
